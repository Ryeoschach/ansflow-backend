apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "standard-app.fullname" . }}-test-connection"
  labels:
    {{- include "standard-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "standard-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
